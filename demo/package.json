{
  "scripts": {
    "dev": "node ./server",
    "// Build": "",
    "prod": "npm run build && npm run serve",
    "build": "npm run build:vite && npm run build:prerender",
    "build:vite": "vite build && vite build --ssr",
    "build:prerender": "vite-plugin-ssr prerender",
    "serve": "serve ./dist/client/ --listen 3000"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "serve": "^11.3.2",
    "vite": "^2.6.14",
    "vite-plugin-ssr": "^0.3.35"
  },
  "name": "demo",
  "version": "0.0.0"
}
